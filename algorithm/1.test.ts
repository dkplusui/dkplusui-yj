function fun(nums: number[], target: number): number {
  function findCenter(arr: number[]): number {
    let result: number = -1;

    return result
  }
  // console.log(());

  return center
}
let arr = [1, 2, 3, 4, 5]
console.log(fun(arr, 2));



// let result = -1;
// for (let i = 0; i < nums.length; i++) {
//   if (nums[i] === target) {
//     result = i
//   }
// }
// return result;